# QonQrete Visual FaQtory v0.5.6-beta — Requirements
# ═══════════════════════════════════════════════════════════════════════════════

# Core (always needed)
pyyaml>=6.0

# Backend: ComfyUI API
requests>=2.28
websocket-client>=1.4

# Image processing (used by mock backend and image metrics)
pillow>=9.0
numpy>=1.24
