# Example capability-split backend config (v0.9.5-beta)
# Keeps the public app interface stable while routing each generation stage
# to a dedicated backend under the hood.

backend:
  type: hybrid
  width: 1024
  height: 576
  timeout: 600

  # Image stage
  image_backend:
    type: comfyui
    api_url: http://localhost:8188
    workflow_image: ./worqspace/workflows/sdxl_t2i.json
    workflow_img2img: ./worqspace/workflows/sdxl_i2i.json

  # Video stage
  video_backend:
    type: comfyui
    api_url: http://localhost:8188
    workflow_video: ./worqspace/workflows/svd_img2vid.json

  # Morph / loop-closure stage
  morph_backend:
    type: comfyui
    api_url: http://localhost:8188
    workflow_morph: ./worqspace/workflows/morph_i2v.json

comfyui:
  sdxl_ckpt: juggernautXL_ragnarokBy.safetensors
  svd_ckpt: svd_xt_1_1.safetensors

# LoRA is validated against the IMAGE capability.
lora:
  enabled: false
  path: /x/ComfyUI/models/loras/pytorch_lora_weights.safetensors
  strength: 1.0
  backend: comfyui

# Local MLX alternative for all three stages:
# backend:
#   type: local
# image_backend:
#   type: local
# video_backend:
#   type: local
# morph_backend:
#   type: local
# local:
#   runner: lightning-mlx
#   models: {image: z-image-turbo, video: wan-2.2, edit: flux-1-kontext}
#   model_paths:
#     flux-1-dev: /path/to/flux-1-dev
#     flux-1-kontext: /path/to/flux-1-kontext
#     wan-2.2: /path/to/wan-2.2
#     z-image-turbo: /path/to/z-image-turbo
